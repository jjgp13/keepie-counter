package com.keepiecounter.detection.ball

import org.junit.Test

/**
 * Tests for BallTracker — will be implemented in Phase 3.
 *
 * Planned test cases:
 * - Ball moving up detected from decreasing Y coordinates
 * - Ball moving down not counted as upward
 * - Peak detection at trajectory inflection point
 * - No false positives with jittery/noisy detections
 * - Handles missing frames gracefully (Kalman interpolation)
 * - Resets cleanly between sessions
 */
class BallTrackerTest {

    @Test
    fun `placeholder - tests will be added in Phase 3`() {
        // BallTracker implementation coming in Phase 3
    }
}
